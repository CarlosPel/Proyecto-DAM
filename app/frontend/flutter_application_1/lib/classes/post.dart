class Post {
  String title;
  String content;
  String datetime;
  String user;

  Post({
    required this.title,
    required this.content,
    required this.datetime,
    required this.user,
  });
}